{% extends 'base.html.twig' %}

{% block title %}Proyecto #{{ project.id }}
{% endblock %}

{% block body %}
<div class="container mt-5">
	<h1>Proyecto:
		{{ project.name }}</h1>

	<table class="table table-bordered">
		<tbody>
			<tr>
				<th>ID</th>
				<td>{{ project.id }}</td>
			</tr>
			<tr>
				<th>Nombre</th>
				<td>{{ project.name }}</td>
			</tr>
			<tr>
				<th>Descripción</th>
				<td>{{ project.description|default('—') }}</td>
			</tr>
		</tbody>
	</table>

	<<ahref="{{path('project_index')}}"class="btnbtn-secondary">←Volverallistado</a><ahref="{{path('project_edit',{id:project.id})}}"class="btnbtn-primary">✎Editar</a>{{include('project/_delete_form.html.twig')}}</div> {% endblock %}
